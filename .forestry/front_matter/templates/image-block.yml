---
label: image block
hide_body: true
fields:
- name: big_image
  type: file
  config:
    maxSize: 250
  label: Big Image
- name: small_image
  type: file
  config:
    maxSize: 250
  label: Small image
